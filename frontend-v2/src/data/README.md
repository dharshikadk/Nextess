# Data-driven UI

Curriculum, badges, directives, leaderboard rows, profile fields and dashboard values are rendered from API/content data. Empty states are intentional and must never be replaced with fake users.